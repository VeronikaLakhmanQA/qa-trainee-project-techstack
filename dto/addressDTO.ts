export interface AddressDTO {
  streetAddress: string;
  city: string;
  state: string;
  zipCode: number;
}

// ToDo: pay attention, code you created and don't use shouldn't be in your PRs and code base.
// for now it's better to create tests for using this DTO
// Otherwise you need to delete this file(it will be worse for you and your experience than create tests)
